# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ feature/githubbuild ]
  pull_request:
    branches: [ feature/githubbuild ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
# https://github.com/actions/setup-java , https://github.com/actions/setup-java/blob/main/docs/advanced-usage.md
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven

# https://docs.github.com/en/actions/learn-github-actions/contexts
#    - name: Dump GitHub context
#      id: github_context_step
#      run: echo '${{ toJSON(github) }}'
#    - name: Dump job context
#      run: echo '${{ toJSON(job) }}'
#    - name: Dump steps context
#      run: echo '${{ toJSON(steps) }}'
#    - name: Dump runner context
#      run: echo '${{ toJSON(runner) }}'
#    - name: Dump strategy context
#      run: echo '${{ toJSON(strategy) }}'
#    - name: Dump matrix context
#      run: echo '${{ toJSON(matrix) }}'

# https://docs.github.com/en/actions/learn-github-actions/environment-variables
#    - name: teststuff
#      run: pwd; ls; echo Hpstestsecret - $HPSTEST; ls $HOME/.m2; cat $HOME/.m2/settings.xml; env | sort ; ls $RUNNER_TOOL_CACHE
#      env:
#        HPSTEST: ${{ secrets.HPSTESTSECRET }}

    - name: Build with Maven
      run: mvn -Ddeploy_password=$NEXUS_BUILD_PASSWD -s .travis.settings.xml -B verify

#    - name: Deploy with Maven
#      run: mvn -Ddeploy_password=$NEXUS_BUILD_PASSWD -s .travis.settings.xml -B deploy
#      env:
#        NEXUS_BUILD_PASSWD: ${{ secrets.NEXUS_BUILD_PASSWD }}
